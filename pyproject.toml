[project]
name = "promptpad"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain_openai",
    "python-dotenv",
    "langchain",
    "langchain_community"
]
